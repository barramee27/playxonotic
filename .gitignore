# =============================================================================
# GitHub tracks only the Xonotic WebAssembly / web port (xonotic-web-port/).
# Other folders in this workspace stay on disk but are never committed.
# =============================================================================

# --- Local-only projects (not part of this public tree) ---
antigravity-game/
antigravity-ac-deb/
antigravity-mod/
playxonotic/
assaultcube/
assaultcube-src/
assaultcube-custom.sh
assaultcube-launcher.sh
quake-mod/
tools/
docs/
.cursor/
.github/
xonotic-data.pk3dir/
3d_model/
sound_assets/
org.xonotic.Xonotic/

# Loose notes at repo root (optional local docs)
/ASSAULTCUBE.md
/CUSTOM_CONTENT_README.md
/PLAY-Custom-AC.md
/play.sh

# Dependencies
node_modules/

# --- Xonotic WASM / Emscripten build outputs (regenerate with build-wasm.sh) ---
xonotic-web-port/emscripten_cache/
xonotic-web-port/emscripten_config
xonotic-web-port/source/darkplaces/build-obj/
xonotic-web-port/source/darkplaces/*.html
xonotic-web-port/source/darkplaces/*.js
xonotic-web-port/source/darkplaces/*.wasm
xonotic-web-port/source/darkplaces/*.data
build_log*.txt

# Environment and secrets — NEVER commit these
.env
.env.*
*.env
*.env.local
*.env.production
*.env.development
.env.*.local
backend.env.*
deploy/*.env*
**/secrets.*
*.secret

# Logs and editor junk
*.log
*.har
.DS_Store
.idea/
.vscode/
t.txt

# Large binary bundles (not source)
*.pk3
*.deb
